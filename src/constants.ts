/**
 * The assumed size of a tile. If a tile layer specifies a different size, it
 * should be scaled to this size in SVG space.
 */
export const TILE_EXTENT = 4096;

export const MIN_ZOOM = 0;

export const MAX_ZOOM = 14;
